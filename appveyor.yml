environment:
  matrix:
    - nodejs_version: "10"

install:
  - ps: Install-Product node $env:nodejs_version

test_script:
  - node --version
  - yarn --version
  - yarn
  - yarn test:appveyor

build: off
